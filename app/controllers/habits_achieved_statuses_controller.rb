class HabitsAchievedStatusesController < ApplicationController
  def edit_achieved_status
    # @targets = current_user.targets.all.includes(:target)
    # @habits = []
    # @targets.each do |target|
    #   @habits << target.habits.all.includes(:habit)
    # end
  end

  def update_achieved_status
    # @habit = Habit.find(habit_id)
    # if @habit
  end

  

end
